import 'package:flutter/material.dart';

class AboutFailure extends StatelessWidget {
  const AboutFailure({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO implement this one
    return Container();
  }
}
