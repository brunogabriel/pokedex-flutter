import 'package:flutter/material.dart';

class DetailsFailure extends StatelessWidget {
  const DetailsFailure({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
